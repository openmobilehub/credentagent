# Private keys never leave this machine. gen-pki.sh writes them here.
keys/
*.csr
# Staged site download artifacts + vendored QR encoder — all build outputs of
# site/build_site.py (source lives in out/ and certs/; the QR lib in node_modules).
site/credentials/
site/trust/
site/certs/
site/vendor/
site/node_modules/
# macOS cruft
.DS_Store
